Since the customer doesn't know, focus on F1 report all (prec, rec)



Good idea to use sentencepiece.


Read about whether one could extract the probabilities for each class
with rnn, cnn and RoBERTa



Minimise the differences between the models, however RoBERTa is pretrained, thus
despite using similar tokenisation, roberta still has prior knowledge. to discuss in methods



Perhaps use codecarbon to get carbon footprint.


Get/examine why the model picks the class it picks, and perhaps the second most likely class.
	Perhaps other models in future work, more transparent like logistic regression, random forest,
	decision tree, Support Vector Machine.

